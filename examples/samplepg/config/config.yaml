app:
  name: samplepg
  port: 7000
  debug:
    config: false
  fiber:
    enable_print_routes: true
    enable_splitting_on_parsers: true
  datastore:
    psql:
      host: localhost
      port: 5432
      username: myapp
      password: myapp
      db: myapp
